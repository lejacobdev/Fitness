/** teamSports movement patterns (see ../poses.js for the format). */
import { ATHLETIC, P, STAND, both, handsToFloor, kf, library, on, reach, reachBoth, side, solve, toeDown, levelFeet } from './kit.js';

const lib = library();
const def = lib.def;

export const TEAM_SPORTS = lib.patterns;
